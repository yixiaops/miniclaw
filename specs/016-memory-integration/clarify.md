# Clarify: 双层记忆系统集成

## 待澄清问题

请回答以下问题，以便细化实施计划。

---

### Q1: 记忆上下文注入机制（P0）

**问题**: MemoryManager 是否需要支持记忆上下文注入到 Agent 的系统提示词？

**子问题**:
- [ ] 是否需要在 Agent.chat() 前自动搜索相关记忆并注入？
- [ ] 注入格式是什么？（如 `<memory_context>...</memory_context>`）
- [ ] 注入长度限制？（避免 token 过多）
- [ ] 注入时机？（每次对话前？还是特定触发条件？）

**选项**:
- **A**: 不注入，Agent 只通过 memory_search 工具主动查询
- **B**: 自动注入，每次 chat 前搜索并注入相关记忆
- **C**: 可选注入，通过配置开关控制

**你的选择**: ______

---

### Q2: 记忆写入时机与对象（P0）

**问题**: 记忆写入时机是什么？写入用户消息、Assistant 响应还是两者都写入？

**子问题**:
- [ ] 写入用户消息？
- [ ] 写入 Assistant 响应？
- [ ] 写入时机：handleMessage 结束后？还是实时写入？
- [ ] 内容粒度：完整消息？还是提取关键信息？

**选项**:
- **A**: 只写入用户消息（用户的意图更重要）
- **B**: 只写入 Assistant 响应（决策和结论更重要）
- **C**: 两者都写入（完整对话记录）
- **D**: 可配置（通过配置决定写入哪些）

**你的选择**: ______

---

### Q3: SimpleMemoryStorage 处理方式（P0）

**问题**: Gateway 现有的 SimpleMemoryStorage 是否完全替换，还是保留作为 session history 存储？

**子问题**:
- [ ] SimpleMemoryStorage 的职责是什么？（session history 持久化）
- [ ] 双层记忆系统是否替代 session history？
- [ ] 是否需要保留两套系统并行？
- [ ] 迁移策略：如何处理现有 session 文件？

**选项**:
- **A**: 完全替换，SimpleMemoryStorage 不再使用
- **B**: 保留并行，SimpleMemoryStorage 用于 session history，双层记忆用于重要信息
- **C**: 合为一套，session history 也用双层记忆存储

**你的选择**: ______

---

### Q4: 默认 importance 分数（P1）

**问题**: 写入短期记忆时的默认 importance 分数如何确定？

**子问题**:
- [ ] 默认值是多少？（0.3？0.5？）
- [ ] 是否需要根据消息类型动态调整？（用户消息 vs Assistant 响应）
- [ ] 是否需要根据消息长度调整？
- [ ] 用户是否可以手动标记重要性？

**选项**:
- **A**: 固定默认值 0.3（低于晋升阈值，需要手动标记才晋升）
- **B**: 固定默认值 0.5（等于晋升阈值，自动晋升）
- **C**: 动态计算（根据消息长度、类型等）
- **D**: 可配置（通过配置设置默认值）

**你的选择**: ______

---

### Q5: memory_get 工具改造（P1）

**问题**: memory_get 工具是否也需要改造使用双层记忆？

**子问题**:
- [ ] memory_get 当前从 memory/*.md 文件读取
- [ ] 双层记忆是否需要支持按 ID 读取特定记忆？
- [ ] memory_get 与 memory_search 的关系是什么？
- [ ] 接口兼容性：现有参数格式是否需要保持？

**选项**:
- **A**: 不改造，memory_get 继续从文件读取（长期记忆都在文件中）
- **B**: 改造，memory_get 通过 MemoryManager 读取（支持短期和长期）
- **C**: 合并，memory_get = memory_search + 指定 ID

**你的选择**: ______

---

### Q6: 默认启用状态（P1）

**问题**: memory.enabled 默认 false，是否会影响现有用户？

**子问题**:
- [ ] 默认 false 是否合理？（向后兼容）
- [ ] 新用户是否需要手动开启？
- [ ] 是否需要提示用户开启新功能？
- [ ] 热更新支持：运行时切换启用状态？

**选项**:
- **A**: 默认 false，保持向后兼容，用户主动开启
- **B**: 默认 true，新功能默认启用，但保留降级开关
- **C**: 根据配置文件自动检测（有 memory 配置则启用）

**你的选择**: ______

---

### Q7: 性能要求（P2）

**问题**: 记忆系统的性能要求是什么？

**子问题**:
- [ ] 写入延迟上限？（建议 < 100ms）
- [ ] 搜索延迟上限？（建议 < 500ms）
- [ ] 并发写入支持？（多 Agent 同时写入）
- [ ] 内存占用上限？

**你的期望**: ______

---

### Q8: 错误处理策略（P2）

**问题**: 记忆系统出错时如何降级？

**子问题**:
- [ ] 写入失败：是否阻断主流程？还是静默失败？
- [ ] 搜索失败：是否返回空结果？还是报错？
- [ ] TTL 清理失败：是否重试？还是跳过？
- [ ] 持久化失败：是否影响关闭流程？

**选项**:
- **A**: 静默降级，不影响主流程（写入失败只记录日志）
- **B**: 阻断主流程，确保数据一致性
- **C**: 可配置（根据配置决定策略）

**你的选择**: ______

---

## 回答模板

请按以下格式回答：

```
Q1: C - 可选注入，通过配置开关控制
Q2: D - 可配置，默认两者都写入
Q3: B - 保留并行，SimpleMemoryStorage 用于 session history
Q4: A - 固定默认值 0.3
Q5: B - 改造，通过 MemoryManager 读取
Q6: A - 默认 false，保持向后兼容
Q7: 写入 < 100ms，搜索 < 500ms
Q8: A - 静默降级，不影响主流程

补充说明：...
```

---

*生成时间: 2026-04-15*